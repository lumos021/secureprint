{
  "name": "root",
  "private": true,
  "scripts": {
  "start": "lerna run start --stream",
  "build": "lerna run build --stream",
  "test": "lerna run test --stream"
},
  "workspaces": [
    "secureprint/backend",
    "secureprint/frontend",
    "secureprint/electron"
  ],
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
